name: Cleanup Docker

on:
  workflow_dispatch:
jobs:
    cleanup:
      runs-on: ubuntu-latest
      steps:
        - uses: dataaxiom/ghcr-cleanup-action@v1
          with:
            delete-untagged: true
